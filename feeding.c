#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--){
            int f=20;
      int x,y;
      scanf("%d%d",&x,&y);
    int a=(x/y);
    if(a>f)printf("20\n");
      else printf("%d\n",a);

    } return 0;
}
